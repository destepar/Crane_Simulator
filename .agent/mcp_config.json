{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "-y",
        "playwright-mcp",
        "--browser", "chrome",
        "--headless",
        "--ignore-https-errors"
      ]
    }
  }
}